const toggle=document.querySelector('[data-menu-toggle]');const menu=document.querySelector('[data-mobile-menu]');if(toggle&&menu){toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));menu.hidden=open;toggle.setAttribute('aria-label',open?'Abrir menu':'Fechar menu')});menu.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{toggle.setAttribute('aria-expanded','false');toggle.setAttribute('aria-label','Abrir menu');menu.hidden=true}))}const select=document.querySelector('[data-service-select]');document.querySelectorAll('[data-service],[data-interest]').forEach(link=>{link.addEventListener('click',()=>{const value=link.dataset.service||link.dataset.interest;if(!select||!value)return;const options=[...select.options];const match=options.find(option=>option.value===value||option.text===value);if(match)select.value=match.value})});const form=document.querySelector('[data-contact-form]');if(form){form.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(form);const nome=String(data.get('nome')||'').trim();const email=String(data.get('email')||'').trim();const servico=String(data.get('servico')||'Ainda não sei').trim();const mensagem=String(data.get('mensagem')||'').trim();const text=[`Olá, Fluxo RH! Meu nome é ${nome}.`,`Meu interesse/momento: ${servico}.`,email?`Meu e-mail: ${email}.`:'',mensagem?`Sobre meu momento profissional: ${mensagem}`:'','Gostaria de entender os próximos passos.'].filter(Boolean).join('\n\n');window.open(`https://wa.me/5592984704754?text=${encodeURIComponent(text)}`,'_blank','noopener')})}const revealObserver='IntersectionObserver'in window?new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');revealObserver.unobserve(entry.target)}})},{threshold:.08}):null;document.querySelectorAll('.reveal').forEach(el=>{if(revealObserver)revealObserver.observe(el);else el.classList.add('visible')});document.querySelectorAll('[data-year]').forEach(el=>{el.textContent=new Date().getFullYear()});